import React, { Component } from 'react';
// import PropTypes from 'prop-types';

class UpcomingSessionsWidget extends Component {
  static propTypes = {
    // children: PropTypes.node.isRequired,
  };

  render() {
    return <div className="widget upcoming-sessions-list-wrapper">upcoming sessions list widget</div>;
  }
}

export default UpcomingSessionsWidget;
